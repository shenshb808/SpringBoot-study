server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shb_db?characterEncoding=utf-8&useSSL=false
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver
  mail:
    host: smtp.qq.com # SMTP 服务器地址
    port: 587 # 服务器的端口
    username: 782846415@qq.com # 配置邮箱用户名
    password: jgxkgpwrwnjwbaih # 配置密码，注意，不是真正的密码，而是刚刚申请到的授权码
    default-encoding: utf-8 # 默认的邮件编码
